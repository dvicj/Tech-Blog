    <div>
        <h2 class="dash-title">dashboard</h2>
        <a href="/dashboard/create/"><button id="create-post">xxx create post xxx</button></a>
{{#if posts.length}}
    </div>

    {{#each posts as |post|}}
        {{> post-info post}}
            <a href="/dashboard/edit/{{post.id}}"><button>xxx edit post xxx</button></a>
        </div>
    {{/each}}

{{else}}
<h6>you do not currently have any posts</h6>
{{/if}}